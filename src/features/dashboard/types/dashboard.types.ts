export type FeatureCardProps = {
  icon: React.ComponentType<{ className?: string }>;
  titleKey: string;
  descKey: string;
};

export type TechCardProps = {
  titleKey: string;
  descKey: string;
};
